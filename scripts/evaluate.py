"""CLI: run experiment grid — executes all configs, saves results JSON per config.

Usage: python scripts/evaluate.py --configs experiments/configs/
                                   --ground-truth data/ground_truth.json
                                   --output results/experiments/

Deliverable D7 (PRD Section 7b): 24+ JSON result files, each with config + metrics.
"""
